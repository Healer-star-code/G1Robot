# 宇树 G1 软件架构与网络配置

> 资料来源：unitree_sdk2、unitree_sdk2_python、Weston Robot Development Guide

---

## 一、网络配置

### 1.1 设备 IP 分配

G1 内部通过 L2 以太网交换机连接所有网络设备。连接到 [4] 或 [5] 号口即可加入同一网络。

| 设备 | IP 地址 | 子网掩码 | 用户名/密码 |
|------|---------|----------|------------|
| 运动控制计算机 | 192.168.123.161 | 255.255.255.0 | 不开放 |
| 开发计算机 (Jetson Orin NX) | 192.168.123.164 | 255.255.255.0 | unitree / 123 |
| Livox Mid-360 激光雷达 | 192.168.123.20 | 255.255.255.0 | N/A |

### 1.2 网络特点

- **无 DHCP 服务器**：外部电脑需手动设置静态 IP
- 设置 IP 为 `192.168.123.x`（x ≠ 161, 164, 20）
- 子网掩码：`255.255.255.0`

### 1.3 外部电脑连接设置

```bash
# 替换 enp131s0 为你的网卡名（用 ip a 查看）
sudo ip addr flush dev enp131s0
sudo ip addr add 192.168.123.200/24 dev enp131s0
sudo ip link set enp131s0 up
```

### 1.4 SSH 登录开发计算机

```bash
ssh unitree@192.168.123.164
# 密码: 123
```

### 1.5 共享互联网给 G1

**在笔记本电脑上：**
```bash
sudo sysctl -w net.ipv4.ip_forward=1

# wlp132s0f0 换成你的 WiFi 网卡名，enp131s0 换成连接 G1 的网卡名
sudo iptables -t nat -A POSTROUTING -o wlp132s0f0 -s 192.168.123.0/24 -j MASQUERADE
sudo iptables -A FORWARD -i wlp132s0f0 -o enp131s0 -m state --state RELATED,ESTABLISHED -j ACCEPT
sudo iptables -A FORWARD -i enp131s0 -o wlp132s0f0 -j ACCEPT
```

**在 G1 上：**
```bash
sudo ip route del default 2>/dev/null || true
sudo ip route add default via 192.168.123.200 dev eth0
echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf

# 验证
ping -c 3 8.8.8.8
```

### 1.6 G1 的 WiFi（可选）

```bash
# 开启 WiFi（默认被禁用）
sudo rfkill unblock all
sudo ip link set wlan0 up
sudo nmcli radio wifi on
sudo nmcli device set wlan0 managed yes
sudo systemctl restart NetworkManager

# 连接 WiFi
sudo nmcli connection add type wifi ifname wlan0 con-name "YourSSID" ssid "YourSSID"
sudo nmcli connection modify "YourSSID" wifi-sec.key-mgmt wpa-psk
sudo nmcli connection modify "YourSSID" wifi-sec.psk "YourPassword"
sudo nmcli connection modify "YourSSID" connection.autoconnect yes
sudo nmcli connection up "YourSSID"
```

---

## 二、软件架构

### 2.1 整体架构图

```
┌────────────────────────────────────┐
│        用户程序（C++ / Python）      │
│   unitree_sdk2  |  unitree_sdk2_py  │
│   unitree_ros2  |  自定义控制器      │
└──────────────┬─────────────────────┘
               │ CycloneDDS (DDS 中间件)
               │ 通过以太网 (192.168.123.x)
┌──────────────┴─────────────────────┐
│        运动控制计算机（不可访问）      │
│   - Unitree 运动控制器               │
│   - 电机驱动                         │
│   - 传感器采集（IMU、电池等）          │
│   - 与电机驱动直接通信                │
└────────────────────────────────────┘
```

### 2.2 核心概念

- **运动控制计算机**：Unitree 文档中也称"运控计算单元"。运行 Unitree 运动控制器，直接与电机驱动器及底层设备通信，**用户不可访问**。
- **开发计算机**：Jetson Orin NX，用户在此运行自己的代码。C++ 和 Python 程序均可运行。

### 2.3 高层控制 vs 底层控制

| 控制级别 | 说明 | 适用场景 |
|----------|------|----------|
| **高层控制** | 控制机器人作为整体（速度、姿态、步态）。运动控制器自动保持平衡 | 下肢运动：站立、蹲下、速度移动、特殊动作 |
| **底层控制** | 直接控制每个关节/电机的 q、dq、tau | 上肢关节控制、全关节协调、研究开发 |

> 从编程角度，使用 Unitree 运动控制器时，G1 可视为一个由腿部驱动的"移动底盘" + 两个手臂的"移动操作平台"。

### 2.4 冲突注意

高层运动控制器和用户底层控制可能同时试图控制同一关节，结果不可预测。必须通过进入**调试模式**禁用运动控制器后再进行底层控制。

---

## 三、软件依赖

### 3.1 C++ SDK

| 依赖 | 版本 |
|------|------|
| Ubuntu | 20.04 LTS |
| CPU | aarch64 / x86_64 |
| GCC | 9.4.0 |
| CMake | >= 3.10 |

安装：
```bash
sudo apt-get update
sudo apt-get install -y cmake g++ build-essential libyaml-cpp-dev \
    libeigen3-dev libboost-all-dev libspdlog-dev libfmt-dev
```

### 3.2 Python SDK

| 依赖 | 版本 |
|------|------|
| Python | >= 3.8 |
| cyclonedds | == 0.10.2 |
| numpy | 最新 |
| opencv-python | 最新（摄像头功能需要）|

### 3.3 CycloneDDS 兼容性注意

G1 Orin 上的默认 CycloneDDS 安装可能不兼容 `unitree_hg` IDL 格式（较新的 G1 数据格式）。

**解决方案**：删除默认安装，按 Unitree ROS2 安装指南重新安装 CycloneDDS。

```bash
git clone https://github.com/eclipse-cyclonedds/cyclonedds -b releases/0.10.x
cd cyclonedds && mkdir build install && cd build
cmake .. -DCMAKE_INSTALL_PREFIX=../install
cmake --build . --target install
```

---

## 四、相关代码仓库

| 仓库 | 用途 | 地址 |
|------|------|------|
| unitree_sdk2 | C++ SDK | https://github.com/unitreerobotics/unitree_sdk2 |
| unitree_sdk2_python | Python SDK | https://github.com/unitreerobotics/unitree_sdk2_python |
| unitree_ros2 | ROS2 驱动 | https://github.com/unitreerobotics/unitree_ros2 |
| unitree_mujoco | MuJoCo 仿真 | https://github.com/unitreerobotics/unitree_mujoco |
| unitree_IL_lerobot | 模仿学习+遥操 | https://github.com/unitreerobotics/unitree_IL_lerobot |
| lerobot | LeRobot 集成 | https://github.com/huggingface/lerobot |
