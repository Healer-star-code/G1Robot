# 宇树 G1 关节控制与电机指令

> 资料来源：unitree_sdk2 C++ SDK (g1_ankle_swing_example.cpp, gamepad.hpp)

---

## 一、关节索引枚举

```cpp
enum G1JointIndex {
    // 左腿 (6 关节)
    LeftHipPitch = 0,
    LeftHipRoll = 1,
    LeftHipYaw = 2,
    LeftKnee = 3,
    LeftAnklePitch = 4,    // PR模式: Pitch, AB模式: Motor B
    LeftAnkleB = 4,
    LeftAnkleRoll = 5,     // PR模式: Roll, AB模式: Motor A
    LeftAnkleA = 5,

    // 右腿 (6 关节)
    RightHipPitch = 6,
    RightHipRoll = 7,
    RightHipYaw = 8,
    RightKnee = 9,
    RightAnklePitch = 10,  // PR模式: Pitch, AB模式: Motor B
    RightAnkleB = 10,
    RightAnkleRoll = 11,   // PR模式: Roll, AB模式: Motor A
    RightAnkleA = 11,

    // 腰部 (3 关节)
    WaistYaw = 12,
    WaistRoll = 13,        // 锁定后无效
    WaistA = 13,           // 锁定后无效
    WaistPitch = 14,       // 锁定后无效
    WaistB = 14,           // 锁定后无效

    // 左臂 (7 关节)
    LeftShoulderPitch = 15,
    LeftShoulderRoll = 16,
    LeftShoulderYaw = 17,
    LeftElbow = 18,
    LeftWristRoll = 19,
    LeftWristPitch = 20,   // 23DOF 无效
    LeftWristYaw = 21,     // 23DOF 无效

    // 右臂 (7 关节)
    RightShoulderPitch = 22,
    RightShoulderRoll = 23,
    RightShoulderYaw = 24,
    RightElbow = 25,
    RightWristRoll = 26,
    RightWristPitch = 27,  // 23DOF 无效
    RightWristYaw = 28     // 23DOF 无效
};

const int G1_NUM_MOTOR = 29;
```

---

## 二、控制律

电机执行的控制律为 **PD + 前馈**：

```
tau_out = kp * (q_des - q_cur) + kd * (dq_des - dq_cur) + tau_ff
```

| 参数 | 字段 | 说明 |
|------|------|------|
| q_des | `MotorCmd_.q` | 目标位置 (rad) |
| dq_des | `MotorCmd_.dq` | 目标速度 (rad/s) |
| tau_ff | `MotorCmd_.tau` | 前馈力矩 (Nm) |
| kp | `MotorCmd_.kp` | 位置刚度 |
| kd | `MotorCmd_.kd` | 速度阻尼 |

---

## 三、推荐增益参数

### 3.1 G1 默认 Kp（位置刚度）

```cpp
std::array<float, 29> Kp{
    60, 60, 60, 100, 40, 40,    // 左腿 (膝=100)
    60, 60, 60, 100, 40, 40,    // 右腿 (膝=100)
    60, 40, 40,                 // 腰部
    40, 40, 40, 40, 40, 40, 40, // 左臂
    40, 40, 40, 40, 40, 40, 40  // 右臂
};
```

### 3.2 G1 默认 Kd（速度阻尼）

```cpp
std::array<float, 29> Kd{
    1, 1, 1, 2, 1, 1,         // 左腿 (膝=2)
    1, 1, 1, 2, 1, 1,         // 右腿 (膝=2)
    1, 1, 1,                  // 腰部
    1, 1, 1, 1, 1, 1, 1,      // 左臂
    1, 1, 1, 1, 1, 1, 1       // 右臂
};
```

### 3.3 增益调整指南

| 场景 | Kp | Kd | 说明 |
|------|-----|-----|------|
| 安全调试 | 10~20 | 0.5~1 | 小刚度，机器人相对柔软 |
| 正常操作 | 40~100 | 1~2 | 参考默认值 |
| 高精度位置 | 100~200 | 2~5 | 刚度增大，但需注意稳定性 |
| 力矩控制 | 0 | 0 | Kp=Kd=0，仅用 tau_ff |

---

## 四、完整控制流程

### 4.1 数据结构准备

```cpp
struct MotorCommand {
    std::array<float, 29> q_target = {};   // 目标位置
    std::array<float, 29> dq_target = {};  // 目标速度
    std::array<float, 29> kp = {};         // 位置增益
    std::array<float, 29> kd = {};         // 速度增益
    std::array<float, 29> tau_ff = {};     // 前馈力矩
};

struct MotorState {
    std::array<float, 29> q = {};          // 当前位置
    std::array<float, 29> dq = {};         // 当前速度
};
```

### 4.2 控制循环 (2ms)

```cpp
void Control() {
    MotorCommand cmd;
    auto state = motor_state_buffer_.GetData();

    // 设置所有关节为默认增益
    for (int i = 0; i < 29; ++i) {
        cmd.tau_ff[i] = 0.0;
        cmd.q_target[i] = 0.0;
        cmd.dq_target[i] = 0.0;
        cmd.kp[i] = Kp[i];
        cmd.kd[i] = Kd[i];
    }

    if (state) {
        // --- 用户自定义控制逻辑 ---

        // 示例：左膝关节保持 0.5 rad
        cmd.q_target[LeftKnee] = 0.5;

        // 示例：力矩控制模式（某关节）
        cmd.kp[RightElbow] = 0;
        cmd.kd[RightElbow] = 0;
        cmd.tau_ff[RightElbow] = 1.0;  // 1 Nm 力矩

        motor_command_buffer_.SetData(cmd);
    }
}
```

### 4.3 指令发送循环 (2ms)

```cpp
void LowCommandWriter() {
    LowCmd_ dds_cmd;
    dds_cmd.mode_pr() = static_cast<uint8_t>(mode_pr_);
    dds_cmd.mode_machine() = mode_machine_;

    auto mc = motor_command_buffer_.GetData();
    if (mc) {
        for (int i = 0; i < 29; i++) {
            dds_cmd.motor_cmd().at(i).mode() = 1;  // 使能
            dds_cmd.motor_cmd().at(i).tau() = mc->tau_ff[i];
            dds_cmd.motor_cmd().at(i).q()   = mc->q_target[i];
            dds_cmd.motor_cmd().at(i).dq()  = mc->dq_target[i];
            dds_cmd.motor_cmd().at(i).kp()  = mc->kp[i];
            dds_cmd.motor_cmd().at(i).kd()  = mc->kd[i];
        }

        dds_cmd.crc() = Crc32Core(
            (uint32_t *)&dds_cmd,
            (sizeof(dds_cmd) >> 2) - 1
        );

        lowcmd_publisher_->Write(dds_cmd);
    }
}
```

---

## 五、踝关节模式切换

踝关节采用并联机构，支持 PR 和 AB 两种模式。

### PR 模式（默认，mode_pr = 0）

直接控制 Pitch 和 Roll 关节：

```cpp
mode_pr_ = Mode::PR;  // 0

// 踝关节指数 [4]=Pitch, [5]=Roll
cmd.q_target[LeftAnklePitch]  = desired_pitch;
cmd.q_target[LeftAnkleRoll]   = desired_roll;
cmd.q_target[RightAnklePitch] = desired_pitch;
cmd.q_target[RightAnkleRoll]  = desired_roll;
```

### AB 模式（mode_pr = 1）

直接控制并联机构的 A 和 B 电机：

```cpp
mode_pr_ = Mode::AB;  // 1

// 注意：AB 模式下索引含义不同
// [4]=B电机(原Pitch), [5]=A电机(原Roll)
cmd.q_target[LeftAnkleB]   = desired_B;
cmd.q_target[LeftAnkleA]   = desired_A;
cmd.q_target[RightAnkleB]  = desired_B;
cmd.q_target[RightAnkleA]  = desired_A;
```

### 模式切换注意

- 切换 `mode_pr` 时需修改 `LowCmd_.mode_pr` 字段
- `mode_machine` 字段是只读的（由机器人自动设置），应读取后原样回传
- AB 模式需要用户自行计算并联机构的运动学关系

---

## 六、电机故障检测

```cpp
// 检测电机状态
for (int i = 0; i < 29; ++i) {
    if (low_state.motor_state()[i].motorstate() != 0) {
        // 该电机故障！motorstate 为非零故障码
        printf("[ERROR] motor %d with code %u\n",
               i, low_state.motor_state()[i].motorstate());
    }
}
```

常见故障码含义需参考宇树文档。

---

## 七、安全编程要点

### 7.1 关节限位保护

```cpp
#include <algorithm>

float clamp_angle(float target, int joint_id) {
    // 使用关节限位表（参见《01-硬件配置与关节参数》）
    const float limits[29][2] = {
        {-2.5307, 2.8798},   // 0
        {-0.5236, 2.9671},   // 1
        // ... 其余关节
    };
    return std::clamp(target, limits[joint_id][0], limits[joint_id][1]);
}
```

### 7.2 平滑过渡

```cpp
// 避免阶跃指令，使用插值
double ratio = std::clamp(elapsed_time / transition_duration, 0.0, 1.0);
target = current_pos * (1.0 - ratio) + desired_pos * ratio;
```

### 7.3 紧急停止

```cpp
// 检测到异常时立即进入阻尼
void emergency_stop() {
    MotorCommand stop_cmd;
    for (int i = 0; i < 29; ++i) {
        stop_cmd.kp[i] = 0;
        stop_cmd.kd[i] = 0;
        stop_cmd.tau_ff[i] = 0;
    }
    motor_command_buffer_.SetData(stop_cmd);
}
```
