# 宇树 G1 遥控器数据结构

> 资料来源：unitree_sdk2 C++ SDK (advanced_gamepad.hpp, gamepad.hpp)、unitree_sdk2_python IDL

---

## 一、两种读取方式

遥控器数据有**两种获取途径**：

| 方式 | 数据来源 | 适用场景 |
|------|----------|----------|
| 独立 DDS 订阅 | Topic `rt/wirelesscontroller` | 高层控制、仅需遥控器状态 |
| 内嵌于 LowState | `LowState_.wireless_remote[40]` | 底层控制时同时读取 |

---

## 二、独立 DDS 订阅方式

### 2.1 Topic 信息

- **Topic 名称**: `rt/wirelesscontroller`
- **消息类型**: `unitree_go::msg::dds_::WirelessController_`

### 2.2 数据结构 (Python IDL)

```python
@dataclass
class WirelessController_:
    lx: float32       # 左摇杆 X 轴 (-1.0 ~ 1.0)
    ly: float32       # 左摇杆 Y 轴 (-1.0 ~ 1.0)
    rx: float32       # 右摇杆 X 轴 (-1.0 ~ 1.0)
    ry: float32       # 右摇杆 Y 轴 (-1.0 ~ 1.0)
    keys: uint16      # 按键位掩码
```

### 2.3 按键位掩码 (keys)

`keys` 是一个 16 位无符号整数，每个 bit 对应一个按键：

| 位 | 按键 | 说明 |
|----|------|------|
| 0 | R1 | 右肩键1 |
| 1 | L1 | 左肩键1 |
| 2 | start | 开始键 |
| 3 | select | 选择键 |
| 4 | R2 | 右肩键2 |
| 5 | L2 | 左肩键2 |
| 6 | F1 | 功能键1 |
| 7 | F2 | 功能键2 |
| 8 | A | A键 |
| 9 | B | B键 |
| 10 | X | X键 |
| 11 | Y | Y键 |
| 12 | up | 方向上 |
| 13 | right | 方向右 |
| 14 | down | 方向下 |
| 15 | left | 方向左 |

**检查按键示例**：
```python
# 检查 A 键是否按下
a_pressed = bool(keys & (1 << 8))

# 检查 L2 是否按下
l2_pressed = bool(keys & (1 << 5))
```

### 2.4 C++ 读取示例

```cpp
#include "unitree/idl/go2/WirelessController_.hpp"
#include "unitree/robot/channel/channel_subscriber.hpp"

#define TOPIC_JOYSTICK "rt/wirelesscontroller"

// 创建订阅器
ChannelSubscriberPtr<unitree_go::msg::dds_::WirelessController_> joystick_subscriber;
joystick_subscriber.reset(
    new ChannelSubscriber<unitree_go::msg::dds_::WirelessController_>(TOPIC_JOYSTICK));
joystick_subscriber->InitChannel(MessageHandler, 1);

// 回调函数
void MessageHandler(const void *message) {
    auto &msg = *(unitree_go::msg::dds_::WirelessController_ *)message;
    float lx = msg.lx();
    float ly = msg.ly();
    float rx = msg.rx();
    float ry = msg.ry();
    uint16_t keys = msg.keys();
}
```

### 2.5 Python 读取示例

```bash
python3 ./example/wireless_controller/wireless_controller.py enp2s0
```

---

## 三、内嵌 LowState 方式（底层控制）

### 3.1 原始数据结构

遥控器原始数据包含在 `LowState_.wireless_remote[40]`（40 字节数组）中。

#### xRockerBtnDataStruct 结构解析

```cpp
// 40 字节原始遥控器数据结构
typedef struct {
    uint8_t head[2];           // 帧头标识
    xKeySwitchUnion btn;       // 按键位掩码 (16bit)
    float lx;                  // 左摇杆 X 值
    float rx;                  // 右摇杆 X 值
    float ry;                  // 右摇杆 Y 值
    float L2;                  // L2 扳机模拟值
    float ly;                  // 左摇杆 Y 值
    uint8_t idle[16];          // 保留字节
} xRockerBtnDataStruct;
```

> 注意：此方式包含 **L2 模拟值**，独立 DDS 订阅方式不含此字段。

#### xKeySwitchUnion 按键联合体

```cpp
typedef union {
    struct {
        uint8_t R1 : 1;
        uint8_t L1 : 1;
        uint8_t start : 1;
        uint8_t select : 1;
        uint8_t R2 : 1;
        uint8_t L2 : 1;
        uint8_t F1 : 1;
        uint8_t F2 : 1;
        uint8_t A : 1;
        uint8_t B : 1;
        uint8_t X : 1;
        uint8_t Y : 1;
        uint8_t up : 1;
        uint8_t right : 1;
        uint8_t down : 1;
        uint8_t left : 1;
    } components;
    uint16_t value;
} xKeySwitchUnion;
```

#### 读取方式

```cpp
// 从 LowState 中读取遥控器数据
REMOTE_DATA_RX rx;
memcpy(rx.buff, &low_state.wireless_remote()[0], 40);

// 读取摇杆值
float left_x  = rx.RF_RX.lx;
float left_y  = rx.RF_RX.ly;
float right_x = rx.RF_RX.rx;
float right_y = rx.RF_RX.ry;
float l2_value = rx.RF_RX.L2;

// 读取按键
bool a_pressed = rx.RF_RX.btn.components.A;
bool l2_pressed = rx.RF_RX.btn.components.L2;
```

---

## 四、Gamepad 工具类

C++ SDK 提供了 `Gamepad` 类封装，支持摇杆**平滑滤波**和**死区**设置。

### 4.1 Gamepad 类接口

```cpp
class Gamepad {
public:
    void update(xRockerBtnDataStruct &key_data);

    float lx, rx, ry, l2, ly;  // 滤波后的摇杆值
    float smooth = 0.03;       // 平滑系数
    float dead_zone = 0.01;    // 死区

    Button R1, L1, start, select, R2, L2;
    Button F1, F2, A, B, X, Y;
    Button up, right, down, left;
};
```

### 4.2 Button 类接口

```cpp
class Button {
public:
    bool pressed;      // 当前是否按下
    bool on_press;     // 刚按下（上升沿）
    bool on_release;   // 刚释放（下降沿）
};
```

### 4.3 使用示例

```cpp
Gamepad gamepad;
gamepad.dead_zone = 0.05;   // 设置死区
gamepad.smooth = 0.2;       // 设置平滑

// 更新
gamepad.update(rx.RF_RX);

// 使用
if (gamepad.A.on_press) {
    // A 键刚被按下
}
float speed = gamepad.ly;  // 左摇杆 Y 控制前进速度
```
