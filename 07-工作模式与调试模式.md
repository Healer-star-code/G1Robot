# 宇树 G1 工作模式与调试模式

> 资料来源：unitree_sdk2 示例代码、GitHub Issues、Weston Robot Guide

---

## 一、工作模式概览

G1 机器人有以下几种核心工作状态：

| 状态 | 说明 | 进入方式 |
|------|------|----------|
| 阻尼模式 (Damp) | 关节无力，可手动摆动 | 开机默认、按 L2+B |
| 位置模式 | 关节保持固定位置 | 按 L2+A（调试模式下） |
| 运动就绪 | ai_sport 服务运行，响应遥控器和 SDK 命令 | L1+A → L1+UP |
| 调试模式 | 关闭运动控制器，用户完全控制 | L2+R2 |

---

## 二、正常启动流程

### 2.1 开机与初始化

1. 机器人上电 → 自动进入**阻尼状态**
2. 按遥控器 **L1 + A** → 系统初始化
3. 按遥控器 **L1 + UP** → 系统进入**运动就绪**状态

此时：
- 可操作遥控器摇杆控制移动
- SDK 高层命令可用（StandUp, Move, ShakeHand 等）
- `ai_sport` 服务运行中

### 2.2 正常关机

在阻尼模式下，短按 + 长按电池开关键进行安全关机。

---

## 三、调试模式（Debug Mode）

调试模式会**关闭 ai_sport 运动客户端**，停止 Unitree 运动控制程序的所有指令发送。

### 3.1 进入调试模式

**前提条件**：G1 处于悬挂状态 + 阻尼状态

1. 同时按下遥控器 **L2 + R2** → 进入调试模式
2. 按下 **L2 + A** → 进入位置模式，机器人摆出特定诊断姿态
3. 按下 **L2 + B** → 返回阻尼状态（可确认调试模式是否成功）

### 3.2 调试模式特点

| 功能 | 调试模式 | 正常模式 |
|------|----------|----------|
| 低层电机控制 (LowCmd) | 可用 | 冲突风险 |
| 高层运动控制 (Move 等) | **不可用** | 可用 |
| 特殊动作 (ShakeHand) | **不可用** | 可用 |
| LED 控制 | 可用 | 可用 |
| 语音合成 (TTS) | 可用 | 可用 |
| 遥控器控制移动 | 不可用 | 可用 |

### 3.3 退出调试模式

调试模式无法简单退出，**需要重启 G1**。

> 在调试模式下，App 中的 DEVICE → SERVICE STATUS → ai_sport/8.0.3.0 状态为灰色（不可操作）。

---

## 四、模式切换流程图

```
     开机上电
        │
        ▼
   [ 阻尼状态 ]
        │
   L1+A │
        ▼
   [ 初始化 ]
        │
  L1+UP │
        ▼
   [ 运动就绪 ]  ←── ai_sport 运行中
        │              │
        │    L2+R2      │  重启
        ▼              │
   [ 调试模式 ]  ──────┘
        │
   L2+A │  L2+B
        ▼    ▼
  [位置]  [阻尼]
```

---

## 五、SDK 中的运动服务管理

### 5.1 MotionSwitcherClient（C++）

在发送低层控制指令前，务必先释放运动控制服务：

```cpp
#include <unitree/robot/b2/motion_switcher/motion_switcher_client.hpp>

auto msc = std::make_shared<unitree::robot::b2::MotionSwitcherClient>();
msc->SetTimeout(5.0f);
msc->Init();

// 循环释放所有正在运行的模式
std::string form, name;
while (msc->CheckMode(form, name), !name.empty()) {
    if (msc->ReleaseMode()) {
        std::cout << "Failed to switch to Release Mode\n";
    }
    sleep(5);
}

// 此时可以安全发送 LowCmd
```

### 5.2 Python 中的服务管理

Python SDK 中通过 DDS Request/Response 来控制服务。

---

## 六、常见问题

### Q1: SDK 命令不生效？

**检查**：
- 是否已进入调试模式？（低层控制需要）
- 是否发送了 LowCmd 同时运动控制器也在运行？（冲突）
- 固件版本是否 >= v1.3.0？

### Q2: 调试模式下高层控制可用吗？

**不可用**。调试模式关闭了 `ai_sport`，高层命令（Move、ShakeHand 等）需要 `ai_sport`。

### Q3: 如何只做 LED 灯控而不影响运动？

LED 控制和 TTS(语音) 不受调试模式影响，正常模式和调试模式都可用。

### Q4: 23 DOF 和 29 DOF 的关节数量不同？

是的。23 DOF 版本的 WristPitch(20/27) 和 WristYaw(21/28) 不存在，在代码中应跳过这些索引。

### Q5: 底层控制时是否必须进入调试模式？

**是**。低层 LowCmd 控制必须进入调试模式。高层控制不需要。

---

## 七、关键注意事项

1. **固件版本要求**：v1.3.0 以上支持完整 LED/Audio 控制
2. **EDU 版本要求**：SDK 开发需要 G1-EDU 版本
3. **CycloneDDS 兼容性**：G1 Orin 的默认 CycloneDDS 可能不兼容 `unitree_hg` 格式，需重新安装
4. **冲突避免**：低层和底层控制不能同时运行
5. **CRC 校验**：LowCmd 必须带正确 CRC，LowState 读取后建议校验
6. **安全操作**：测试新算法时从小力矩/低速度开始，随时准备急停
